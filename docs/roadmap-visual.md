# Roadmap visual

```
KV (done) -> Relational TS (done) -> Relational Zig (done fase 1)
  -> scanRange -> pushdown -> catalog persist -> bench reproducible
  -> row checksums -> SQL amplio (sin copiar motores lentos)
```
