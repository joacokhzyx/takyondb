# Packaging relacional

Sin cambios: `takyondb` + bridge + SDK `dist/` (ahora incluye `relational/`).
- Linux `.deb`, macOS `.pkg`, Windows `.exe` desde CI artifacts.
- NPM publica `dist/` con `relational/*.d.ts`.
