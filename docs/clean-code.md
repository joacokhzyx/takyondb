# Código limpio (relacional)

- Headers MIT obligatorios, `zig fmt` + ESLint verdes.
- Funciones pequeñas, sin allocs ocultas (documenta `Allocator`).
- Tests por módulo, mocks sin SHM real para TS.
- Commits convencionales con DCO (`-s`).
