# Siguientes pasos (fase 2)

1. `scanRange` nativo + raíces ART múltiples.
2. Pushdown SIMD + agregación vectorizada.
3. Catálogo persistente + checksums.
4. Bench reproducible + `SharedArrayBuffer` real.
