# Guía de contribución relacional

- Un módulo = un commit (`feat(sdk): ...`, `feat(core): ...`, `test(...): ...`).
- Todo archivo nuevo lleva header MIT + tests.
- `zig fmt` antes de cada commit Zig; `npm run test:unit` antes de cada commit TS.
- Firma DCO: `git commit -s`.
